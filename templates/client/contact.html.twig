    {% extends 'base.html.twig' %}

    {% block title %}LACVI - Nous contacter{% endblock %}

    {% block body %}

    <main class="w-full flex-grow flex flex-col justify-start items-center gap-8 px-7 py-8 lg:px-16 lg:grid lg:grid-cols-3">
        <div class="w-full h-full flex flex-col p-6 gap-4 lg:col-span-1 lg:border-r-2 lg:border-gray">
            <h3 class="text-red">Nous contacter</h3>
            <p>Vous avez une question, vous ne trouvez pas un article, le site affiche une erreur ? Envoyez-nous un mail pour que nous y répondions.</p>
        </div>

        <!--Form Section-->
        {{ form_start(contactForm, {
            'attr': {'class': 'w-full h-full flex flex-col items-center justify-start gap-4 lg:col-span-2'}
        }) }}

            {{ form_errors(contactForm) }}

            <div class="w-full flex flex-col gap-2">
                {{ form_label(contactForm.subject) }}
                {{ form_widget(contactForm.subject, {'attr': {
                    'class': 'px-4 py-2 shadow-inner rounded-sm bg-gray/10',
                    'placeholder': 'Objet',
                }}) }}
                {{ form_errors(contactForm.subject) }}
            </div>

            <div class="w-full flex flex-col gap-2">
                {{ form_label(contactForm.message) }}
                {{ form_widget(contactForm.message, {'attr': {
                    'class': 'px-4 py-2 shadow-inner rounded-sm bg-gray/10 min-h-48',
                    'placeholder': 'Message',
                }}) }}
                {{ form_errors(contactForm.message) }}
            </div>

            {{ form_widget(contactForm.submit, {'attr': {'class': 'w-fit h-fit px-6 py-3 rounded-sm bg-red text-white shadow-box cursor-pointer'} }) }}

        {{ form_end(contactForm) }}
    </main>

    {% endblock %}