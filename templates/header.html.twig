    {% block header %}

    <header class="w-full h-fit flex justify-between gap-4 px-4 py-3 bg-white shadow-box lg:px-16">
        <a href="{{ path('home') }}" class="max-w-1/2 w-[180px]">
            <img src="{{ asset('img/logo.png') }}" alt="Logo" class="w-full" />
        </a>

        <div class="w-fit flex items-center gap-3">
            <a href="{{ path('profile') }}" class="p-2">
                <img src="{{ asset('img/icons/person.svg') }}" alt="Profil" />
            </a>
            <a href="{{ path('basket') }}" class="p-2">
                <img src="{{ asset('img/icons/shopping_cart.svg') }}" alt="Panier" />
            </a>
            <a href="{{ path('about') }}" class="p-2">
                <img src="{{ asset('img/icons/info.svg') }}" alt="À propos" />
            </a>
        </div>
    </header>

    {% endblock %}
